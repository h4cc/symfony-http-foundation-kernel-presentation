% START preamble

% Do not textit comments, because it will make $ go £
%\renewcommand{\CommentTok}[1]{\textcolor[rgb]{0.56,0.35,0.01}{#1}}

% Extra modules
\usepackage{wrapfig}
\usepackage{multicol}

% Disable the navigation bar on the lower right corner.
\usenavigationsymbolstemplate{}

% STOP preamble